# Production Team vCluster - Advanced Networking
sync:
  toHost:
    ingresses:
      enabled: true
    services:
      enabled: true
  fromHost:
    nodes:
      enabled: true  # Sync real nodes for scheduling

# Cross-vCluster DNS resolution
controlPlane:
  coredns:
    enabled: true
