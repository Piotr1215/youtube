apiVersion: v1
kind: Pod
metadata:
  name: kai-cpu-pod
  labels:
    kai.scheduler/queue: test
spec:
  schedulerName: kai-scheduler
  containers:
  - name: main
    image: busybox
    command: ["sleep", "3600"]
    resources:
      requests:
        cpu: "100m"
        memory: "64Mi"
      limits:
        cpu: "200m"
        memory: "128Mi"
